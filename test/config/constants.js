const DEMO = false;
const DEFAULT_HEADLESS = true;
const DEFAULT_TINEOUT = 30 * 1000;
const DEFAULT_SLO_MO = 100;
const DEFAULT_VIEWPORT = {
  width: 1200,
  height: 800,
};

module.exports = {
  DEMO,
  DEFAULT_HEADLESS,
  DEFAULT_TINEOUT,
  DEFAULT_SLO_MO,
  DEFAULT_VIEWPORT,
};
